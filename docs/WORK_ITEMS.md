# Work item references

The audit remediation work is tracked in GitHub issues `#58`–`#121` in
`Collaboration95/talktoit`; the stable IDs and acceptance criteria are recorded
in the repository's remediation plan. Older comments use these aliases:

| Alias | Meaning |
| --- | --- |
| `R1-*` | First product review pass; superseded by the issue index in `plan.md`. |
| `GH-*` | Historical GitHub issue reference; use the current issue number in `plan.md`. |
| `A-*` | Prior audit items documented in `AUDIT.md`; status is recorded in `audit/README.md`. |

New code should describe the invariant directly and use the current issue
number when a tracker reference is needed.
