# Work item references

The audit remediation work is tracked in GitHub issues `#58`–`#121` in
`Collaboration95/talktoit`; each issue records its stable ID, severity, and
acceptance criteria. Older comments use these aliases:

| Alias | Meaning |
| --- | --- |
| `R1-*` | First product review pass; superseded by the issues in `#58`-`#121`. |
| `GH-*` | Historical GitHub issue reference; use the current issue number. |
| `A-*` | Prior audit items documented in `AUDIT.md`. |

New code should describe the invariant directly and use the current issue
number when a tracker reference is needed.
